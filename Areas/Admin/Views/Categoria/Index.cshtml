@model IEnumerable<AAAAA_FUNCIONA_MDS.Models.Categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Categorias</h1>

<p>
    <a asp-action="Create">Criar novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeCategoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <style>
        {
            
        }
    </style>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCategoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NomeCategoria">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.NomeCategoria">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.NomeCategoria">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
